import 'package:flutter/material.dart';

class ThesisTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Tab(
      text: "Community Thesis"
    );
  }
  
}