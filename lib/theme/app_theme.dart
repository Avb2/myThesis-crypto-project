import 'package:flutter/material.dart';

class AppTheme {
  static ThemeData theme = ThemeData(
      primaryColor: const Color.fromARGB(255, 204, 234, 249),
        colorScheme: ColorScheme.fromSeed(seedColor: const Color.fromARGB(255, 0, 49, 78)),
        useMaterial3: true,
      );
}